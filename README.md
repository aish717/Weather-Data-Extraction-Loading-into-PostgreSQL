# Weather-Data-Extraction-Loading-into-PostgreSQL
This is a project based on data ETL where we can extract load and save the data in the table in the PostgreSql and use pgadmin GUI  to view the tables
